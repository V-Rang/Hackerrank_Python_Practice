from itertools import combinations
ls = input().split()
instr = ls[0]
instr = sorted(instr)
k = int(ls[1])
for x in range(1,k+1):
    for y in list(combinations(instr,x)):
        for z in range(len(y)):
            print(y[z],end='')
        print()
