from itertools import combinations_with_replacement
ls = input().split()
instr = ls[0]
instr = sorted(instr)
k = int(ls[1])
# for x in range(1,k+1):
for y in list(combinations_with_replacement(instr,k)):
    for z in range(len(y)):
        print(y[z],end='')
    print()
